- create wrapper object to store/expose attributes of grids:
  - corner points
  - middle point
  - area?
  - average elevation?
  (shapely)
- improve the CLI wrapper
  (docopt)
- add tests
  (pytest)

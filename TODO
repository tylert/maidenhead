- replace all the sys.exit junk with proper exception stuff
- create wrapper object to store/expose attributes of grids:
  - corner points
  - middle point
  - area?
  - average elevation?
- improve the CLI wrapper
  (docopt)
- add tests
  (pytest)
